<h1>管理者トップページ</h1>

<%= link_to "ユーザ作成(管理者/ワーカー)", new_admin_user_path %>
<%= link_to "大タスク一覧" , admin_missions_path %>
<%= link_to "小タスク一覧" , admin_tasks_path %>


<!-- ボタン押したら「ユーザ」「管理者」「ワーカ」で切り替わるとええかも -->

<h2>ユーザー</h2>
<table>
  <thead>
    <tr>
      <th>名前</th>
      <th>メールアドレス</th>
      <th>プロジェクト名</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>

        <% if user.projects.present?  %>
          <% user.projects.each do |project| %>

            <td><%= link_to project.name , admin_user_project_path(user,project) %></td>

          <% end %>
        <% else %>
          <td>登録なし</td>
        <% end %>
        
        <td><%= link_to "プロジェクト登録", new_admin_user_project_path(user) %></td>
        <td><%= link_to "ユーザー編集", edit_admin_user_path(user) %></td>
        <td><%= link_to "ユーザー削除", admin_user_path(user), method: :delete, data: { confirm: "本当に削除してもよろしいですか？" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<h2>管理者</h2>
<table>
  <thead>
    <tr>
      <th>名前</th>
      <th>メールアドレス</th>
    </tr>
  </thead>
  <tbody>
    <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.name %></td>
        <td><%= admin.email %></td>
        <td><%= link_to "編集", edit_admin_user_path(admin) %></td>
        <td><%= link_to "削除", admin_user_path(admin), method: :delete, data: { confirm: "本当に削除してもよろしいですか？" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<h2>ワーカー</h2>
<table>
  <thead>
    <tr>
      <th>名前</th>
      <th>メールアドレス</th>
    </tr>
  </thead>
  <tbody>
    <% @workers.each do |worker| %>
      <tr>
        <td><%= worker.name %></td>
        <td><%= worker.email %></td>
        <td><%= link_to "編集", edit_admin_user_path(worker) %></td>
        <td><%= link_to "削除", admin_user_path(worker), method: :delete, data: { confirm: "本当に削除してもよろしいですか？" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


