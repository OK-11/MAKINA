<%= form_with(model: [:admin, mission], local: true) do |form| %>

  <div>
    <%= form.label :name, "大タスク名" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "登録" %>
  </div>

<% end %>